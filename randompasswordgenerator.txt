import random


def pass_word(pass_length):
    characters='ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz1234567890!@#$%^&*._'
    pass_list=[]

    for char in pass_length:
        password=""
        for j in range(char):
            next_index = random.randrange(len(characters))
            password = password + characters[next_index]
        password =  rand_pos(password)
        pass_list.append(password)
    return pass_list


def rand_pos(passwrd):
    for i in range(random.randint(1, 8)):
        return passwrd


def main():
    numPasswords = int(input("How many passwords do you want to generate? "))
    print("Generating " + str(numPasswords) + " passwords")
    passwordLengths = []
    print("Minimum length of password should be 3")

    for i in range(numPasswords):
        length = int(input("Enter the length of Password #" + str(i + 1) + " "))

        def condition():
            passwordLengths.append(length)
            Password = pass_word(passwordLengths)
            for i in range(numPasswords):
                print("Password #" + str(i + 1) + " = " + Password[i])

        if length>=3:
            condition()
        else:
            print("sorry!")
            ans=input("would you like to generate password with three characters: ")
            if ans in ['Y','Yes','YES','y','yes']:
                length=3
                condition()
            else:
                break
main()